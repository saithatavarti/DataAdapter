parameters:
    adapter: mssql+pyodbc
    username : LAPTOP-CMNLMDRF\SQLEXPRESS
    database : students
    drivers: SQL Server
    address : http://127.0.0.1:8000/

query:
  code: select * from details
  user: select name from username where name =