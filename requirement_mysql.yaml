development:
  db_type: mysql
  database: sys
  adapter: mysql
  username: root
  password: root
  port: 1521
  url: http://127.0.0.1:8000/
  drivers: SQL Server

#Custom query
query:
  code: select * from auction
  user: select name from user where name =

count_query:
  SELECT COUNT(*) AS count FROM students_sqlite

authentication_query:
  select name from users_dataadapter where name =