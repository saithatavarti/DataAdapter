parameters:

    database_port:     5432
    database_user:     postgres
    adapter: postgresql
    username : postgres
    password : chillale
    database : krish
    address : http://127.0.0.1:8000/

query:
  code : select * from student
  user: select * from username where name =
 

user:
  select * from username